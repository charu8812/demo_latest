spring:
  data:
    mongodb:
 #     authentication-database:
      #username: chandan
      #password: root
      database: categoryDB
      port: 27017
      host: localhost
  application:
    name: category-service
server:
  port: 8083
